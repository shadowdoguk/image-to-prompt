# Triage Labels

Default triage label vocabulary (no overrides configured):

| Role | Label |
|------|-------|
| needs-triage | `needs-triage` |
| needs-info | `needs-info` |
| ready-for-agent | `ready-for-agent` |
| ready-for-human | `ready-for-human` |
| wontfix | `wontfix` |

The `triage` skill will apply these labels directly. If your repo already uses different label names, edit this file to map them.